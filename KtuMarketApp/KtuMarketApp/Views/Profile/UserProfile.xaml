<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KtuMarketApp.Views.Profile.UserProfile"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" x:Name="UserProfileContainer">

            <!-- Kullanıcı Profili -->
            <StackLayout>
                <Image Source="{Binding UserPhotoUrl}" BackgroundColor="Aqua"></Image>
            </StackLayout>

            <Label Text="{Binding PersonName, StringFormat='@{0}'}" BackgroundColor="Bisque" HorizontalTextAlignment="Center" FontSize="30"></Label>

            <Button Text="Şifre Değiştir"></Button>
            <Button Text="Hesabı Sil" Clicked="DeletePerson"></Button>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>